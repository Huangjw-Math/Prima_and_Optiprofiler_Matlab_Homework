### Issues
1.The command
```
testprima_ex
``` 
will run the 
```
setup
```
function, which has already been done. 

### Cautions
1.Matlab Online provide a virtual environment to run Matlab codes in Linux environment with almost all standard packages installed. However, with absence of parallel computing environment, running 
```
example1
example2
example3
example4
```
will result in an error.

When we run the code instead locally, the problem won't appear. But running the testing code without the parallel computing package will costs a lot of time.
